#include <bits/stdc++.h>
using namespace std;


int main() {
   vector <int> v(5);

    cin>>v[0] >> v[1] >> v[2] >> v[3];
    int n = v.size();
    int largest = v[n-1];
    for(int i=n-2; i<=0;i--){
        if(v[i] )
    }

}